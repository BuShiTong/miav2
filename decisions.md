# Decision Log

Each decision is logged with reasoning so we can backtrack if needed.

| # | What | Why | Alternative Rejected | Rollback |
|---|------|-----|---------------------|----------|
| 1 | Python backend (not TS/Go/Java) | google-adk is Python-first; TS SDK is alpha, Go/Java have no ADK | Wait for TS SDK to mature | Switch to TS if ADK SDK stabilizes |
| 2 | Explicit search sub-agent with gemini-2.5-flash-lite | ADK source confirms bypass_multi_tools_limit creates sub-agent using PARENT model (native audio), which can't do generateContent | Use bypass_multi_tools_limit=True | If ADK fixes bypass to respect model param, switch to bypass |
| 3 | 3-line Mia instruction (no food safety temps, no decision trees) | Model knows cooking from training data; over-prompting creates rigidity | Detailed prompts like v2 (~200 lines) | Add specific instructions only when testing proves the model needs them |
| 4 | No v2 workarounds in vanilla | Test hypothesis that v2 problems were self-inflicted | Include known-good workarounds from v2 | Add specific workaround when testing proves it's needed, one commit each |
| 5 | Separate agent.py + tools.py (not all in main.py) | Single-responsibility: main.py = server, agent.py = agent definition, tools.py = tool functions. Easier to read and modify | Everything in main.py (~300 lines) | Merge back if separation causes import issues |
| 6 | asyncio.Queue for tool→frontend events | Tools run inside ADK callbacks with no WebSocket access; queue bridges tool execution to the event forwarder task | Direct WebSocket send from tools (not possible — no access to ws) | If queue causes dropped events, increase maxsize or switch to unbounded |
| 7 | Vanilla tool returns (no empty {} hack) | v2 returned empty {} from preferences to suppress duplicate audio. Testing whether this is actually needed | Return empty {} like v2 | Add empty return only if duplicate audio confirmed in testing |
| 8 | No timer limits, no label sanitization | v2 had max 3 timers and label sanitization. Testing whether the model self-regulates | Add limits like v2 | Add specific limits only if testing shows problems |
| 9 | Migrate from ADK to raw google-genai SDK | ADK can't set NON_BLOCKING/WHEN_IDLE scheduling; SDK gives full Live API control; 14 features unavailable in ADK | Stay on ADK + after_tool_callback workaround | git checkout 8f6231d restores working ADK version |
| 10 | Switch from Vertex AI to AI Studio | Vertex AI doesn't support NON_BLOCKING function calling; AI Studio does | Stay on Vertex AI | Switch .env back to Vertex config |
| 11 | Use gemini-2.5-flash-native-audio-latest (GA) | Confirmed available on AI Studio via models.list(); GA = stable, no preview | gemini-2.5-flash-native-audio-preview-12-2025 | Try other GA model IDs if this one fails with Live API |
| 12 | Delete agent.py, keep main.py + tools.py | SDK has no Agent concept; instruction goes in LiveConnectConfig; tools.py stays for tool functions | Keep agent.py as a config module | Re-create if needed |
| 13 | BLOCKING behavior (not NON_BLOCKING) for tools | NON_BLOCKING caused double/triple responses: model spoke before tool ran, then again after WHEN_IDLE result. Our tools are instant (<1ms, in-memory), so BLOCKING has no perceptible pause. One response per tool batch. | NON_BLOCKING + WHEN_IDLE (caused double audio) | Change behavior back to NON_BLOCKING if tools become slow (e.g. network calls) |
| 14 | Re-test NON_BLOCKING + WHEN_IDLE | BLOCKING ignores the scheduling param entirely (confirmed in SDK docs), causing Mia to narrate tool lifecycle redundantly. Re-testing NON_BLOCKING + WHEN_IDLE now that system instruction and transcription handling are improved. | BLOCKING + WHEN_IDLE (scheduling ignored, caused repetition) | Revert to BLOCKING if double responses return |
| 15 | Try BLOCKING + SILENT | Test whether SILENT scheduling reduces repetition with BLOCKING tools. SDK docs say scheduling is ignored with BLOCKING, but testing to verify. Model keeps full tool results for context. | NON_BLOCKING + WHEN_IDLE (untested), BLOCKING + empty {} (loses context) | Switch to empty {} response if SILENT has no effect |
| 16 | Remove enable_affective_dialog AND proactivity (Step 1 rolled back) | API rejects both: "Unknown name" errors. Both fields exist in SDK v1.64.0 and Context7 docs, but server doesn't accept them for gemini-2.5-flash-native-audio-latest. Lesson: SDK fields ≠ API support — always test after adding config flags. | Keep them and hope for API update | Re-add if Google enables these fields for this model |
| 17 | Add VAD config with LOW speech sensitivity | False barge-in: Gemini's voice detection too sensitive — kitchen noise and ambient sounds triggered interruptions, chopping Mia's responses after ~3 words. LOW sensitivity = less false triggers. | No VAD config (default sensitivity) | Bump to HIGH if users find it too hard to interrupt Mia |
| 18 | Switch back to Vertex AI (reverses Decision 10) | Vertex AI now supports gemini-live-2.5-flash-native-audio (stable, non-preview). Vertex-only features work: enable_affective_dialog (model reacts to tone) and proactivity (smarter response timing) — both rejected on AI Studio. Known bug (BLOCKING/SILENT stripped) solved with system prompt + client-side audio gate (0ms latency, 100% reliable). | Stay on AI Studio (BLOCKING/SILENT work natively) | Revert to AI Studio: git checkout 13c9978, restore .env with GOOGLE_API_KEY |
| 19 | System prompt + audio gate for tool behavior (Strategy #1) | Vertex strips BLOCKING/SILENT params from protobuf — model narrates before tool calls and repeats after. Audio gate mutes browser audio during tool execution, unmutes when real response arrives. System prompt adds ~33% compliance, gate adds 100%. Model still receives full tool results. | Manual Activity Interruption (+100-200ms, requires rebuilding VAD), Client Turn Claim (audio glitches), Prompt Only (~33% effective) | Remove audio gate logic from downstream() if Vertex fixes the BLOCKING/SILENT bug |
| 20 | Remove proactivity (keep affective dialog) | proactivity=True caused model to silently hold back responses — user spoke but got complete silence. affective_dialog works fine. | Keep proactivity enabled | Re-add if Google fixes proactivity behavior for native audio |
| 21 | Change greeting trigger to explicit instruction | "Hello" was too ambiguous — model interpreted it as a user question and called search_web on session start. Changed to "[Session started — greet the user]" so model knows it's a system signal. | Keep "Hello" as greeting trigger | Revert to "Hello" if new trigger causes issues |
| 22 | Remove restart timer action | Restart used adjusted remaining time instead of original duration. User doesn't need it. Removed from tool declaration, handler, and system prompt. | Fix restart to store original_duration | Re-add if users request it |
| 23 | Timer deduplication (same label within 10s) | Model called manage_timer(set, rice) twice in 550ms, creating duplicate timers. Dedup returns "already set" if timer with same label exists and was created within 10 seconds. | No dedup (trust model) | Remove dedup if it blocks legitimate same-label timers |
| 24 | 4-state audio gate (replaces boolean gate) | Boolean gate reset on turn_complete between tool call waves, missed duplicate responses. State machine: NORMAL→BLOCKING→PASSING→COOLDOWN→NORMAL. BLOCKING stays on through tool call waves. COOLDOWN catches duplicate responses within 3s. 500ms batch window prevents premature transitions. | Keep boolean gate | Revert to boolean gate: git checkout 63a8ee7 |
| 25 | Strengthen search filler instruction | Model inconsistently said filler before search_web — sometimes silent for 4+ seconds. Made instruction more emphatic: "You MUST say filler FIRST. Never call search_web without speaking first." | Keep current soft instruction | Soften instruction if model over-fillers |
