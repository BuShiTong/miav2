# Decision Log

Each decision is logged with reasoning so we can backtrack if needed.

| # | What | Why | Alternative Rejected | Rollback |
|---|------|-----|---------------------|----------|
| 1 | Python backend (not TS/Go/Java) | google-adk is Python-first; TS SDK is alpha, Go/Java have no ADK | Wait for TS SDK to mature | Switch to TS if ADK SDK stabilizes |
| 2 | Explicit search sub-agent with gemini-2.5-flash-lite | ADK source confirms bypass_multi_tools_limit creates sub-agent using PARENT model (native audio), which can't do generateContent | Use bypass_multi_tools_limit=True | If ADK fixes bypass to respect model param, switch to bypass |
| 3 | 3-line Mia instruction (no food safety temps, no decision trees) | Model knows cooking from training data; over-prompting creates rigidity | Detailed prompts like v2 (~200 lines) | Add specific instructions only when testing proves the model needs them |
| 4 | No v2 workarounds in vanilla | Test hypothesis that v2 problems were self-inflicted | Include known-good workarounds from v2 | Add specific workaround when testing proves it's needed, one commit each |
| 5 | Separate agent.py + tools.py (not all in main.py) | Single-responsibility: main.py = server, agent.py = agent definition, tools.py = tool functions. Easier to read and modify | Everything in main.py (~300 lines) | Merge back if separation causes import issues |
| 6 | asyncio.Queue for tool→frontend events | Tools run inside ADK callbacks with no WebSocket access; queue bridges tool execution to the event forwarder task | Direct WebSocket send from tools (not possible — no access to ws) | If queue causes dropped events, increase maxsize or switch to unbounded |
| 7 | Vanilla tool returns (no empty {} hack) | v2 returned empty {} from preferences to suppress duplicate audio. Testing whether this is actually needed | Return empty {} like v2 | Add empty return only if duplicate audio confirmed in testing |
| 8 | No timer limits, no label sanitization | v2 had max 3 timers and label sanitization. Testing whether the model self-regulates | Add limits like v2 | Add specific limits only if testing shows problems |
| 9 | Migrate from ADK to raw google-genai SDK | ADK can't set NON_BLOCKING/WHEN_IDLE scheduling; SDK gives full Live API control; 14 features unavailable in ADK | Stay on ADK + after_tool_callback workaround | git checkout 8f6231d restores working ADK version |
| 10 | Switch from Vertex AI to AI Studio | Vertex AI doesn't support NON_BLOCKING function calling; AI Studio does | Stay on Vertex AI | Switch .env back to Vertex config |
| 11 | Use gemini-2.5-flash-native-audio-latest (GA) | Confirmed available on AI Studio via models.list(); GA = stable, no preview | gemini-2.5-flash-native-audio-preview-12-2025 | Try other GA model IDs if this one fails with Live API |
| 12 | Delete agent.py, keep main.py + tools.py | SDK has no Agent concept; instruction goes in LiveConnectConfig; tools.py stays for tool functions | Keep agent.py as a config module | Re-create if needed |
| 13 | BLOCKING behavior (not NON_BLOCKING) for tools | NON_BLOCKING caused double/triple responses: model spoke before tool ran, then again after WHEN_IDLE result. Our tools are instant (<1ms, in-memory), so BLOCKING has no perceptible pause. One response per tool batch. | NON_BLOCKING + WHEN_IDLE (caused double audio) | Change behavior back to NON_BLOCKING if tools become slow (e.g. network calls) |
| 14 | Re-test NON_BLOCKING + WHEN_IDLE | BLOCKING ignores the scheduling param entirely (confirmed in SDK docs), causing Mia to narrate tool lifecycle redundantly. Re-testing NON_BLOCKING + WHEN_IDLE now that system instruction and transcription handling are improved. | BLOCKING + WHEN_IDLE (scheduling ignored, caused repetition) | Revert to BLOCKING if double responses return |
| 15 | Try BLOCKING + SILENT | Test whether SILENT scheduling reduces repetition with BLOCKING tools. SDK docs say scheduling is ignored with BLOCKING, but testing to verify. Model keeps full tool results for context. | NON_BLOCKING + WHEN_IDLE (untested), BLOCKING + empty {} (loses context) | Switch to empty {} response if SILENT has no effect |
| 16 | Remove enable_affective_dialog AND proactivity (Step 1 rolled back) | API rejects both: "Unknown name" errors. Both fields exist in SDK v1.64.0 and Context7 docs, but server doesn't accept them for gemini-2.5-flash-native-audio-latest. Lesson: SDK fields ≠ API support — always test after adding config flags. | Keep them and hope for API update | Re-add if Google enables these fields for this model |
| 17 | Add VAD config with LOW speech sensitivity | False barge-in: Gemini's voice detection too sensitive — kitchen noise and ambient sounds triggered interruptions, chopping Mia's responses after ~3 words. LOW sensitivity = less false triggers. | No VAD config (default sensitivity) | Bump to HIGH if users find it too hard to interrupt Mia |
| 18 | Switch back to Vertex AI (reverses Decision 10) | Vertex AI now supports gemini-live-2.5-flash-native-audio (stable, non-preview). Vertex-only features work: enable_affective_dialog (model reacts to tone) and proactivity (smarter response timing) — both rejected on AI Studio. Known bug (BLOCKING/SILENT stripped) solved with system prompt + client-side audio gate (0ms latency, 100% reliable). | Stay on AI Studio (BLOCKING/SILENT work natively) | Revert to AI Studio: git checkout 13c9978, restore .env with GOOGLE_API_KEY |
| 19 | System prompt + audio gate for tool behavior (Strategy #1) | Vertex strips BLOCKING/SILENT params from protobuf — model narrates before tool calls and repeats after. Audio gate mutes browser audio during tool execution, unmutes when real response arrives. System prompt adds ~33% compliance, gate adds 100%. Model still receives full tool results. | Manual Activity Interruption (+100-200ms, requires rebuilding VAD), Client Turn Claim (audio glitches), Prompt Only (~33% effective) | Remove audio gate logic from downstream() if Vertex fixes the BLOCKING/SILENT bug |
| 20 | Remove proactivity (keep affective dialog) | proactivity=True caused model to silently hold back responses — user spoke but got complete silence. affective_dialog works fine. | Keep proactivity enabled | Re-add if Google fixes proactivity behavior for native audio |
| 21 | Change greeting trigger to explicit instruction | "Hello" was too ambiguous — model interpreted it as a user question and called search_web on session start. Changed to "[Session started — greet the user]" so model knows it's a system signal. | Keep "Hello" as greeting trigger | Revert to "Hello" if new trigger causes issues |
| 22 | Remove restart timer action | Restart used adjusted remaining time instead of original duration. User doesn't need it. Removed from tool declaration, handler, and system prompt. | Fix restart to store original_duration | Re-add if users request it |
| 23 | Timer deduplication (same label within 10s) | Model called manage_timer(set, rice) twice in 550ms, creating duplicate timers. Dedup returns "already set" if timer with same label exists and was created within 10 seconds. | No dedup (trust model) | Remove dedup if it blocks legitimate same-label timers |
| 24 | 4-state audio gate (replaces boolean gate) | Boolean gate reset on turn_complete between tool call waves, missed duplicate responses. State machine: NORMAL→BLOCKING→PASSING→COOLDOWN→NORMAL. BLOCKING stays on through tool call waves. COOLDOWN catches duplicate responses within 3s. 500ms batch window prevents premature transitions. | Keep boolean gate | Revert to boolean gate: git checkout 63a8ee7 |
| 25 | Strengthen search filler instruction | Model inconsistently said filler before search_web — sometimes silent for 4+ seconds. Made instruction more emphatic: "You MUST say filler FIRST. Never call search_web without speaking first." | Keep current soft instruction | Soften instruction if model over-fillers |
| 26 | Remove audio gate entirely (reverses Decision 24) | 4-state gate caused garbled "on that for you!" fragments 6 times in session 9afc4f61. Gate let tail fragments through during PASSING phase while dropping real responses in COOLDOWN. Occasional double-talk (complete sentences) is better than garbled fragments. | Keep 4-state gate | Re-add gate only if Vertex fixes BLOCKING/SILENT support |
| 27 | Server-side tool call validation layer | Model hallucinated preferences (saved "vegetarian" when user said "bolognese"), resumed timers autonomously, paused timers unprompted. Prompt-only controls don't work. Validation checks tool calls against user transcription using keyword matching before executing. Rejects bad calls with {"status": "skipped"} so model moves on silently. | Rely on system prompt only | Remove validation if false rejection rate > 30% |
| 28 | Tool call buffering (300ms) | Model sends tool calls 200ms apart. Buffer collects all calls, waits for transcription to arrive (~3ms after), then validates and responds in one batch. Reduces duplicate responses and enables transcription-based validation. | Process tool calls immediately | Reduce buffer to 100ms if latency is noticeable |
| 29 | Remove timer status action | User tested across 3 sessions — never accurate. Inherent latency: tool returns seconds at call time, model speaks seconds later. Also reported phantom 10-minute timer (empty label). | Keep status and improve prompt | Re-add if Google improves Live API tool response timing |
| 30 | Require timer labels | Model set a timer with empty label, making it invisible on frontend and impossible to reference by name. Server now rejects empty labels with an error. | Allow empty labels | Remove if model frequently needs unnamed timers |
| 31 | Remove "SILENT EXECUTION" from tool descriptions | "SILENT EXECUTION" confused the model into saying garbled filler ("let me check on that for you!") before preference/timer tools. Simplified to plain descriptions. Server-side validation handles behavior enforcement. | Keep SILENT EXECUTION instruction | Re-add if model stops calling tools proactively enough |
| 32 | Simplify system prompt (remove CRITICAL TOOL BEHAVIOR RULES) | 7 lines of tool behavior rules that the model wasn't following. Replaced with one line: "Only use tools when the user explicitly asks." Server-side validation now enforces behavior programmatically. | Keep verbose tool rules | Add specific rules back only if testing shows a gap that validation can't catch |
| 33 | Safety embedding — inject preferences into conversation | Allergies/dietary saved via tool call drift out of context in long sessions. Now re-injected as context message after each preference update. Prevents model from suggesting allergens 15 minutes into a session. | Rely on tool result staying in context | Remove if Gemini Live gets system instruction updates mid-session |
| 34 | Preference negation + comma splitting + merge | "No allergies" was being saved as allergy value. "nuts,shellfish" wasn't split. New prefs replaced old ones. Now: negation removes, commas split, list keys merge. | Keep simple key=value replacement | Revert if model stops sending compound values |
| 35 | Timer validation hardening | No limits on duration (model could set 999999s timer), label content (prompt injection risk), or timer count. Now: 1s-8h range, label sanitized (50 char, no special chars), max 5 timers. | Trust model output | Remove limits if they cause usability issues |
| 36 | Input size caps | No limits on WebSocket message size or image data. Now: 10K char text cap, ~2MB image cap. Prevents memory issues from malformed/oversized payloads. | Trust client input | Adjust limits if legitimate use cases hit them |
| 37 | Duration formatting in timer responses | Model had to convert seconds to human-readable (600 → "10 minutes"). Now tool returns `duration_display` / `remaining_display`. Reduces model math errors. | Let model do the conversion | Remove if it causes the model to read the formatted string awkwardly |
| 38 | Camera vision instructions in system prompt | "Comment on what you see when useful" was too vague. Added specific guidance: cooking progress, safety warnings, don't narrate everything. | Keep minimal instruction | Adjust based on testing — may need more or fewer examples |
| 39 | Playful error messages | Generic tech errors ("Connection failed") replaced with personality-driven messages ("Mia's kitchen is closed", "connection gremlins"). Makes demo errors memorable instead of forgettable. | Keep generic messages | Revert if users find them confusing |
| 40 | Session receive re-entry limit | session.receive() iterator could end silently and loop forever. Now limited to 5 re-entries without data before sending gemini_disconnected. Prevents frozen app. | Infinite retry | Increase limit if legitimate reconnects hit it |
| 41 | PWA manifest | Added manifest.json for mobile installability ("Add to Home Screen"). Makes the app feel native during mobile demos. | No manifest | Remove if PWA behavior causes issues |
| 42 | Context window compression (100K/80K) + session resumption + system instruction updates + GoAway handling | Without compression, camera sessions crash in ~7.5 min (258 tokens/sec). Without session resumption, connections die every ~10 min and Mia loses all context. System instruction updates survive compression (unlike regular turns — confirmed in Vertex docs). GoAway gives 60s warning for graceful reconnection. prefixTurns not available on Vertex. Custom compaction not possible (Live API history immutable). | prefixTurns (not on Vertex), custom compaction (history immutable), ADK EventsCompactionConfig (requires ADK, not raw SDK), regular context reinforcement via role="user" (gets discarded by compression) | Remove context_window_compression if Vertex rejects. Remove session_resumption if reconnect issues. Adjust trigger/target tokens if compression is too aggressive or too late. |
| 43 | Parallelize frontend startup (code check + speakers + mic) | Sequential awaits wasted ~200-600ms: code verification, playback setup, and mic setup ran one after another but don't depend on each other. Now run via Promise.all. WebSocket connect() stays after code validation to protect Vertex AI billing. Also removed playback worklet cache-busting (?v=Date.now()) so browser caches the file on repeat visits. | Pre-warm Vertex AI session on page load (saves ~10-50ms more but adds complexity + billing risk for users who never click Start), remove access code entirely (repo is public, need token protection) | Revert to sequential: git checkout a4b7a89. If Promise.all causes issues with mic permission timing, move startMic() back to sequential after the parallel block. |
| 44 | Native Google Search (replaces flash-lite sub-model) | Lower latency (no round-trip to separate model), simpler code (~60 lines removed), one fewer API call. Live API docs confirm google_search works as native tool alongside function_declarations. Search is transparent — model handles it internally. | Keep separate flash-lite model (works but adds latency and complexity) | git revert 8ff23a5; frontend search infrastructure untouched |
| 45 | 40ms audio chunks (was 100ms) | Vertex best practices recommend 20-40ms. Lower latency for barge-in and model VAD. 25 messages/sec (was 10). BASELINE_ALPHA adjusted 0.05→0.02 to preserve ~2s EMA time constant at higher chunk rate. | 20ms (too many messages, marginal benefit), keep 100ms (works but higher latency) | git revert cbb8874 |
| 46 | Preference persistence across Gemini crashes (E4) | Allergies/dietary stored in tool_state.preferences are lost when Gemini's connection resets (~10 min). After reconnect, periodic system instruction updates have nothing to inject, and after compression the original preference turns get discarded. Safety gap: Mia could suggest allergens in long sessions. Fix: module-level `_saved_preferences` dict keyed by user_id, restored on reconnect, cleared on clean disconnect. Crash vs clean detection: upstream finishes first = user Stop (clear), downstream finishes first = Gemini died (preserve). | Rely on session resumption alone (partial — preferences lost after compression), persist to file/DB (overkill for single-server demo) | Remove `_saved_preferences` dict and crash detection logic from main.py |
| 47 | Resume token age tracking and cleanup (E10) | `_resume_handles` dict grew unboundedly — one entry per unique user, never pruned. Now stores `(handle, timestamp)` tuples. Expired handles (>24h, matching Vertex validity) pruned inline when new handles arrive. No background task needed. | Background cleanup task every 60s (v3 approach — adds complexity), no cleanup (works but leaks memory over time) | Revert `_resume_handles` to plain `dict[str, str]` |
| 48 | WebSocket send timeout — 5s (L6) | All `websocket.send_text()` calls had no timeout. If a browser tab crashes or network dies without clean WebSocket close, send blocks indefinitely — tying up the Gemini Live session doing nothing. Now: `_safe_send()` helper wraps every send in `asyncio.wait_for(..., timeout=5.0)`. On timeout, logs warning and breaks out of the loop, triggering cleanup. No impact on healthy connections. | No timeout (current — works but leaks resources on zombie clients), per-send try/except RuntimeError (catches some cases but not network-level hangs) | Remove `_safe_send()` helper and revert to direct `websocket.send_text()` calls |
| 49 | Health endpoint (N3) | `GET /health` returns `{"status": "ok"}`. Pure liveness probe — no internal state leaked, no logging on hits (prevents probe spam). Useful for quick "is the server running?" checks and required by load balancers/Docker health checks. | Deep health check that pings Gemini (adds coupling and false negatives), no health endpoint (current — must open WebSocket to test) | Remove the `/health` route from main.py |
| 50 | WebSocket ID validation (N8) | `user_id` and `session_id` path params validated against `^[a-zA-Z0-9_-]+$` with 128-char max. Prevents log injection (IDs logged directly at connection) and path issues (session_id used in log filenames). Rejected IDs truncated to 32 chars in warning log to prevent the very injection being guarded against. Close code 1008 (Policy Violation). | No validation (current — accepts any string), validate at HTTP level before accept (less compatible, no close code) | Remove `_VALID_ID` regex and validation block from websocket_endpoint |
| 51 | [DEBUG] Google Search grounding metadata logging | Native Google Search runs transparently inside the model — no way to confirm it actually fires. SDK confirms `LiveServerContent.grounding_metadata` has `web_search_queries`, `grounding_chunks`, `grounding_supports`. Added `[SEARCH]` log lines to `downstream()` to detect when grounding fires and what queries were used. **Temporary debug code — remove before production deploy.** | Trust the model's verbal claims ("I searched for...") — unreliable, no proof. Test with trick questions — manual, not repeatable. | Delete the `[DEBUG] Google Search grounding detection` block from main.py (grep for `[SEARCH]`) |
